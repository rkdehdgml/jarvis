# ─────────────────────────────────────────
#  JARVIS Core AI — Python dependencies
# ─────────────────────────────────────────

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# AI / LLM
anthropic==0.40.0
openai==1.57.0
google-genai==1.20.0
# langchain==0.3.9            # 미사용 — 필요 시 활성화
# langchain-anthropic==0.3.3
# langchain-openai==0.2.11
# langchain-community==0.3.9

# Local LLM (Ollama bridge)
ollama==0.4.4

# chromadb==0.5.23            # 벡터DB — 미구현, 필요 시 활성화

# Speech-to-Text
faster-whisper==1.1.0

# Text-to-Speech
edge-tts>=6.1.9

# Audio capture
sounddevice==0.5.1

# Computer Vision
opencv-python==4.10.0.84
mediapipe==0.10.30

# Desktop automation
pyautogui==0.9.54
pyperclip==1.9.0

# Browser automation (PC 제어 에이전트 — 기존 Chrome 프로필 재사용)
selenium==4.27.1

# 미디어 다운로드
yt-dlp>=2024.12.13
instaloader==4.13.1

# 시스템 제어 (볼륨/오디오) — Windows 전용
pycaw==20240210; sys_platform == "win32"
comtypes==1.4.6; sys_platform == "win32"

# 유틸리티 (QR 생성, 네트워크 속도 측정, PDF 텍스트 추출)
qrcode==8.0
speedtest-cli==2.1.3
pypdf==5.1.0

# 내장 명령 (app/commands) — 위키피디아 검색, 프로그래밍 농담, WhatsApp 전송
wikipedia==1.4.0
pyjokes==0.8.3
pywhatkit==5.4

# HTTP client
requests==2.32.3
httpx==0.28.0

# Config & env
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.7.0

# Utilities
numpy==2.1.3
Pillow==11.0.0
websockets==14.1
psutil>=5.9.0
beautifulsoup4>=4.12.0
